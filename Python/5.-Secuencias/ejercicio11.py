def intercambia(p: tuple) -> tuple:
    # Intercambiamos los dos elementos de la tupla
    return (p[1], p[0])

# Ejemplos de uso
print(intercambia((2, 5)))  # Salida: (5, 2)
print(intercambia((5, 2)))  # Salida: (2, 5)
